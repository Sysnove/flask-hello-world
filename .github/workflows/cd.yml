---
name: "cd"

on:
  workflow_run:
    workflows: ["ci"]
    branches: [master]
    types: 
      - completed
jobs:
  deploy-release:
    name: "Deploy Release"
    runs-on: "ubuntu-latest"

    steps:
    - uses: actions/download-artifact@v3

