---
name: "deploy-release"

on:
  push:
    tags:
      - "**"

jobs:
  tagged-release:
    name: "Tagged Release"
    runs-on: "ubuntu-latest"

    steps:
      # ...
      - name: "Build & test"
        run: |
          echo "done!"
          
      - uses: robinraju/release-downloader@v1.7
        with:
          repository: "DONTMERGE/flask-hello-world"
          latest: true
          tarBall: true
          zipBall: true
